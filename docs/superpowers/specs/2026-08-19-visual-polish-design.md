# 视觉打磨：Hero 场景升级 + 全站广播层 + 完成度修复

日期：2026-08-19
状态：已与作者对齐（方案 A+B+修 bug；C 只做便宜项）
背景：站点交互仪式感强（CLI 开机 → 3D 电视 → 滚动推进 → NO SIGNAL 换台），但穿过开场后正文视觉密度低，"空白读起来像没做完而不是留白"。本轮只做视觉与动效，**所有文案维持现状当占位符**，内容结构（案例页、时间线）不在本轮范围。

诊断依据：19 张全站 headless 截图（桌面 1440×900 + 移动 390×844，日/夜、各 section、子页、聚焦态）。

---

## A · Hero 3D 场景升级

涉及：`src/three/tv.ts`、`src/three/stage.ts`、`src/three/screen.ts`

### A1 电视主体

- **占比加大（已定档）**：相机起点 `startPos.z` 由 10.5 拉近至 **约 8.3**（视觉宽度约 +27%；五档实机截图对比后作者选定）。效果：电视成为画面主角、屏幕打字内容裸眼可读、柜面线上抬使下半屏空白带变窄。`endPos`/`endLook` 与 `labelAnchor`、`knobWorld` 等锚点随之校准；hero 大标题（DOM overlay）上移对位，与电视顶保持呼吸间距，不得重叠。实现时可在 z 8.0–8.6 间微调，以 1440×900 与 390×844 两档构图都成立为准。
- **建模细节**（保持程序化建模、无外部资产；机身已是 `RoundedBoxGeometry` 倒角外壳，倒角不是本轮任务）：
  - V 形天线：两根细圆柱 + 底座半球，位于机顶后部。
  - 第二颗旋钮（音量）：比主旋钮小一号、不加入 raycast 可点击集合（可点击的仍只有主旋钮，避免"长得一样却点不动"）；主旋钮周围加刻度环（环形排列的短刻度块）。
  - 侧板质感（**可选项，实现时先做对比再定**）：CanvasTexture 程序化木纹，与现有奶油色极简气质可能冲突；备选是保持纯色 + 侧面装饰凹槽线。以对比截图择优。
- 现有元素保留：扬声器格栅、拨杆、指示灯、脚垫、电源线、屏幕。

### A2 柜面道具

低多边形、同色系（跟随日/夜主题色变化），摆在电视两侧不抢主体：

- 叠放录像带 3–4 盘（薄盒 + 浅色标签面，微错位旋转）。
- 遥控器（小圆角盒 + 按钮点阵）。
- 小盆栽（花盆圆台 + 2–3 层锥形叶簇）。
- 墙面挂画/挂钟为**可选项**，默认不做（防杂乱）。

### A3 光影

- **日间**（治"白天太素"）：
  - 暖色窗光 SpotLight，`spotlight.map` 用 canvas 画窗格图案，投斜方形窗光斑到墙面与柜面；`castShadow` 开。
  - 电视落在柜面的软阴影加强（shadow radius / 强度调参）。
- **夜间**：
  - 屏幕辉光 PointLight（teal 色调），强度随屏幕内容做轻微 flicker（低频噪声，幅度小，reduced-motion 时静止）。
  - spot 光锥内加尘埃粒子（THREE.Points，数量少、缓慢漂浮，仅夜间可见；reduced-motion 时不动）。
- **柜面微反射**：TV 的倒置镜像副本，其上叠一层柜面色渐变遮罩平面压暗（TV 是多材质 group，不做整组渐变透明），不做实时反射。
- 日/夜切换时新增光源与道具颜色全部纳入现有 `setTheme` 的 GSAP 过渡。

### A4 屏幕内容（dolly 终点不再空）

- `screen.ts` 增加**测试卡模式**：滚动进度 p 超过阈值（约 0.65，实现时调参）后，屏幕内容淡切为经典电视测试卡——同心校准圆 + 色条 + 网格 + `CH 00` 字样 + cry 像素 logo。雪花转场前的特写一瞬有完整画面可看。数据通路：`stage.setProgress` 把进度透传给 `screenFX`（新增接口），绘制函数按模式分支。测试卡在 560×460 逻辑分辨率下须清晰。
- 平时待机画面底部加**节目条跑马灯**：`正在播出：作品集 · CH 00` 循环滚动，丰富屏幕层次。
- 频道编号统一约定：电视屏幕 = 开机频道 `CH 00`，正文各区 = `CH 01–05`（见 B1）；现有待机画面角标里的 `CH 03` 一并改为 `CH 00`。
- 屏幕纹理仍为 CanvasTexture 实时绘制，测试卡为绘制函数分支，不引入图片资产。

### A5 开机与信号叙事（本轮新增，作者选定全做）

- **CRT 开机瞬间**：preloader 收起、进入 hero 的那一刻，屏幕播放经典 CRT 开机动画——黑屏 → 中心一条水平亮线 → 纵向展开 → 亮闪稳定成正常画面，同步一声合成开机音（`sound.ts` 新增 `poweron`，WebAudio 合成，延续无音频文件原则）。把「CLI 输入 `/cry` 回车 = 打开这台电视」的因果闭环做实。时序挂在 `runPreloader` resolve 后；`reduced-motion` 或返回访客（condensed boot）直接呈现稳定画面。
- **headline "信号调入"**：hero 大标题入场时 RGB 通道错位一闪归位（CSS 一次性动画，text-shadow 双色偏移收敛；`reduced-motion` 直显）。
- **dolly 信号失稳**：镜头推进时屏幕内容随进度出现垂直滚动失真（v-hold：画面按进度加剧的周期性纵向偏移/撕裂条），暗示"信号即将切换"，与测试卡切入、NO SIGNAL 雪花构成完整叙事链。进度复用 A4 的 `setProgress` 通路；`reduced-motion` 关闭失真。
- **点击屏幕彩蛋**：`stage` 的 click raycast 命中 `screen` 时（现只处理 knob），屏幕闪一帧雪花噪点并立即切换像素图标（glyph），可反复触发。配一声短 static 音。
- **待机自动换台**（2026-08-20 作者反馈追加：测试卡不能是等人翻页的死帧，"电视自己会播"）：停留在 dolly 终点约 3.2s 不动时，测试卡上出现倒计时条与「即将自动换台 — 滚动可打断」提示，随后自动滚入正文（沿用接缝既有 NO SIGNAL 编舞）；任何滚动/触摸/按键立即打断；每次访问至多自动一次；reduced-motion 不启用。测试卡本身永不静止：雷达扫线、漂移亮带、走秒时间码、呼吸状态行。

涉及：`index.html`、`src/styles/main.css`、`src/gallery.ts`、`src/about.ts`、少量新 fx 代码

### B1 频道角标（统一语言）

每个 section 固定位置（左上或右上）出现 mono 角标，格式统一：

| 区域 | 角标 |
|---|---|
| #about | `CH 01 · 关于` |
| #works | `CH 02 · 精选作品` |
| #manifesto | `CH 03 · 宣言` |
| #footer | `CH 04 · 联系` |
| 子页 #page-about | `CH 05 · 关于 cry` |

进入 section 时角标做一次短暂"跳台"闪烁（CSS animation；reduced-motion 时直接显示）。此编号体系为将来"案例频道页"铺概念，本轮纯视觉。注：早前搁置的内容方案曾设想按项目编频道号（6 个作品各一台），与本轮按 section 编号是两套体系——角标是纯装饰、改动成本低，内容轮启动时统一编号即可，本轮不为此设计兼容。

### B2 频段刻度尺

视口右缘固定竖向刻度尺（细 tick marks + 每 section 一个粗刻度），滚动时高亮当前频段位置，与 B1 角标联动（少量 JS 读 ScrollTrigger 进度）。移动端隐藏。

### B3 扫描线底纹

`#content` 背景在现有网格上叠加极淡横向扫描线（CSS repeating-linear-gradient，透明度 ≤3%），压暗大面积空黑的"未完成感"。

### B4 海报重绘 ×3

数据结构不变（`content.ts` 不动），只改 `gallery.ts decor()` 与对应 CSS：

- **血与火（fire）**：细线 `🜂` 三角 → 像素三头龙徽记（CSS grid 像素画）+ 底部火焰上升渐变；保持红黑基调。
- **浴血黑帮（peaky）**：黑上黑 → 牛皮纸档案质感：米色纸卡底、红线串联的空照片框 2–3 个、加大并旋转的"机密"章、订书钉/回形针细节。未聚焦变暗后仍需轮廓可辨。
- **拼豆（beads）**：off 珠透明度大幅降低（近不可见），爱心珠加径向高光成珠子质感，让爱心剪影立起来。

### B5 子页蓝图重画

`#blueprint` SVG 从"方框+十字"重画为 **CRT 电视解剖线稿**：机身正面轮廓、屏幕圆角矩形、显像管侧视锥体、天线、2–3 条标注引线与尺寸标注。沿用现有 `pathLength=100` + stroke-dashoffset 滚动绘制机制（about.ts 的 `seg()` 进度映射重排给新元素）；现有 24 条程序化放射线取消，由标注引线与刻度按进度逐条出现替代。四角"能力 #1–4"标签的引线指向电视对应部位。文案不动。

### B6 空白支撑元素

- #about 右下：信号强度条纹样（阶梯 bars）+ 频率读数装饰字。
- #manifesto 背景：超大号 `CH 03` 极淡水印（在 echo 层之下）。
- 子页 hero：现有轴线加坐标刻度与光标读数装饰字。
- #footer：只加 B1 角标，保持克制。

---

## C · 微交互（只做便宜项）

- 海报 hover"通电"：亮度短脉冲 + 1px 亮线自上而下扫过（纯 CSS，reduced-motion 关闭）。
- `.pill` 按钮 hover：边框亮起 + 轻微内发光。

---

## D · 完成度修复（全修）

1. **子页蓝链接**：胶囊按钮样式从 `#cta`（ID 选择器）迁移到 `.pill` 类级，子页「联系我」恢复胶囊外观；`#cta` 保留布局差异部分。
2. **「交汇点」断词**：不用 `word-break: keep-all`（纯 CJK 长句无空格，keep-all 会禁止换行导致大字号标题溢出）。改为**预设断行点**：打字机数据（stem/words）中标记允许断行的位置，渲染时在断点处换行；断点固定，逐字打字过程中不因 reflow 漂移。词组（如"交汇点"）永不跨行。
3. **works intro 与 HUD 重叠**：#works 头部布局让位（intro 下移/加顶距），滚动过程中不再与固定 HUD 文字叠字。
4. **CRY logo 日间对比度**：日间 `--hud` 色加深或 logo 点阵加不透明度，顶部中央 logo 清晰可见。
5. **NO SIGNAL 后台冻结**：`nosignal.ts` 监听 `visibilitychange`，页面恢复可见时**若转场仍在播则直接快进完成并清屏**（注意：现有时钟按帧间隔差值累计且 cap 64ms，后台期间时钟暂停，"超时"永远不会自然发生，不能以超时为条件），杜绝黑屏残留盖页。

---

## 范围外（明确不做）

- 一切文案改写（现有文字全部视为占位符，内容轮另行处理）
- 案例页 / 路由 / 时间线等内容结构
- 移动端专项重设计（仅保证本轮改动在 390 宽度不回归）
- 真实时反射、后处理管线（bloom 等）、外部 3D/图片资产

## 验收

- `npm run build`（tsc + vite）通过。
- headless 截图脚本同机位前后对比：日间 hero 不再"下半屏空白 + 小电视"（电视视觉宽度 +27% 档）；dolly 终点为测试卡；三张海报重绘生效；子页蓝图为电视解剖线稿；子页「联系我」为胶囊按钮。
- A5 叙事链完整可感：开机动画→打字待机→推进失稳→测试卡→NO SIGNAL 换台；`reduced-motion` 下全部直达稳定态；点击屏幕彩蛋可重复触发且不干扰旋钮点击。
- 日/夜切换过渡平滑，新增光源/道具颜色随主题。
- `prefers-reduced-motion` 下：新增动效全部静止或直显，无功能缺失。
- WebGL 不可用降级路径不回归（stage try/catch 分支仍工作）。
- 移动端（390 宽）各 section 无布局破损，B2 刻度尺隐藏。
- 性能：新增光源/粒子/镜像后，桌面滚动与日夜切换无肉眼可感掉帧；尘埃粒子设数量上限（数百级），移动端粒子减半或关闭。
